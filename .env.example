OPENAI_API_KEY=
API_MODEL=gpt-6-astra
# OpenAI official endpoint; replace only when using another compatible server.
OPENAI_BASE_URL=https://api.openai.com/v1
ANTHROPIC_API_KEY=

# MangoInference uses its own key; do not replace the OpenAI credentials above.
MANGOINFERENCE_API_KEY=
MANGOINFERENCE_BASE_URL=https://api.mangoboost.io/v1

# Qwen comparison slice (OpenAI-compatible server); separate from cloud provider keys.
QWEN_BASE_URL=
QWEN_MODEL=
QWEN_API_KEY=

# Detector evaluation (separate allocation from generation).
GEMINI_API_KEY=
KANANA_API_KEY=
