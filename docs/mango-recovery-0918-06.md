# 720건 생성 복구 — 0918-06

스트리밍 수신, 장문 출력 한도 확대, 오류 분류와 제한된 재시도를 적용하고 실제 장문 시험 후 본 생성을 재개했다. 전체 720건 완료 보고는 아니다.

## 실제 시험

| 시험 | 결과 | 추정 비용 |
|---|---|---:|
| v6, 16k 대출 약정, 65,536 출력 한도 | stop·usage 수신, 태그 검증 통과. 실제 8,735 근사 토큰으로 장문 하한 미달, 장문 완료 건수 제외 | $0.187249 |
| v7, 고객 8명 16k 민원, 동일 전송 설정 | stop·usage 수신, 본문 분량 충분. 태그 중첩/닫힘 오류 발견 | $0.123199 |
| 위 민원 원문 1회 보정 | stop·usage 수신, 실제 12,513 근사 토큰, 자동 검증 통과 | $0.177525 |

보정본 내용 점검에서 슬롯 밖 영문 이름을 발견했다. 해당 시험본은 원본/계보를 보존하되 최종 통과본 집계에서 격리했다. 문서 계획은 새 실행에서 재생성 대상이다. 자동 검증만으로 모든 PII 누락·의미 오류를 찾을 수 없다는 제한은 유지된다.

## 재개 설정

- 위치: `data/corpus/main-720-v2/recovery-0918-06/main`
- 모델: MangoInference `zai-org/GLM-5.2-FP8`, compose_v7
- 최초 6건 보존, 그 외 714개 계획 처리. 2160건 자동 확대 없음.
- 동시 1건, 스트리밍과 사용량 수신. 16k 계획은 출력 상한 65,536; 나머지는 32,000.
- HTTP 429/500/502/503/529는 지연 후 한 번 재시도. 첫 실패 예약액도 유지. 결과 불확실한 연결 오류는 자동 재시도하지 않음.
- 완성본의 검증 실패는 한 번 보정. 서버 오류·출력 잘림·주석 오류를 별도 집계.
- 기존 실행과 시험의 정산/예약 합계 $2.192789를 이월하고 모든 추가 요청까지 총 $150에서 제한. 실제 제공자 청구 내역과는 구분.
- 실제 재개 후 `kiii-main-v2-00003`의 스트리밍 조각 수신을 확인했다. 지속 상태는 실행 폴더 `summary.json`, 요청별 `state.json`과 `partial.*.json` 참조.

69개 오프라인 테스트 통과. 예산 차감·기존 결과 건너뛰기·불확실 비용 보존·스트림 사용량·장문 하한·격리 표본 제외를 확인했다.

공급자의 용량/장애를 클라이언트 수정으로 없앨 수는 없다. 이번 시험으로 스트리밍 전송과 확대된 출력 한도에서 장문을 완성하고 보정할 수 있음을 확인했으며, 재발 시 원인별 중단 기록을 남긴다. [공식 추론 문서](https://inference.mangoboost.io/docs/guides/reasoning/), [공식 오류 처리 문서](https://inference.mangoboost.io/docs/guides/reliability-and-error-handling/).
